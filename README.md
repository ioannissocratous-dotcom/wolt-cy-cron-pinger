# Wolt CY cron pinger (public)

Lightweight public repo — GitHub scheduled workflows run more reliably here than on private repos.

Dispatches `daily-snapshot` to `ioannissocratous-dotcom/wolt-cy-campaigns-cron` if today's snapshot is missing.
